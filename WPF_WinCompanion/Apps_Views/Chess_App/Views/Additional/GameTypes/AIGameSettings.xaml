<UserControl x:Class="WPF_WinCompanion.Apps_Views.Chess_App.Views.Additional.GameTypes.AIGameSettings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WPF_WinCompanion.Apps_Views.Chess_App.Views.Additional.GameTypes"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="SettingsButton" TargetType="Button">
            <Setter Property="Background" Value="#3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}"
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="#80000000"
            VerticalAlignment="Center"
            Height="350"
            CornerRadius="15"
            BorderBrush="#E74C3C"
            BorderThickness="3">
        <Border.Effect>
            <DropShadowEffect Color="Black" 
                              Direction="315" 
                              ShadowDepth="10" 
                              Opacity="0.8"
                              BlurRadius="15"/>
        </Border.Effect>
        
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="20">
            <TextBlock Text="🤖 AI Game Settings" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Foreground="White" 
                       HorizontalAlignment="Center" 
                       Margin="0,0,0,20"/>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
                <TextBlock Text="AI Difficulty:" 
                           FontSize="16" 
                           Foreground="White" 
                           VerticalAlignment="Center" 
                           Margin="0,0,10,0"/>
                <ComboBox x:Name="DifficultyComboBox" 
                          Width="120" 
                          SelectedIndex="1">
                    <ComboBoxItem Content="Beginner"/>
                    <ComboBoxItem Content="Intermediate"/>
                    <ComboBoxItem Content="Advanced"/>
                    <ComboBoxItem Content="Expert"/>
                </ComboBox>
            </StackPanel>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
                <TextBlock Text="Your Color:" 
                           FontSize="16" 
                           Foreground="White" 
                           VerticalAlignment="Center" 
                           Margin="0,0,10,0"/>
                <ComboBox x:Name="PlayerColorComboBox" 
                          Width="120" 
                          SelectedIndex="0">
                    <ComboBoxItem Content="White"/>
                    <ComboBoxItem Content="Black"/>
                    <ComboBoxItem Content="Random"/>
                </ComboBox>
            </StackPanel>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
                <TextBlock Text="Time Control:" 
                           FontSize="16" 
                           Foreground="White" 
                           VerticalAlignment="Center" 
                           Margin="0,0,10,0"/>
                <ComboBox x:Name="TimeControlComboBox" 
                          Width="120" 
                          SelectedIndex="2">
                    <ComboBoxItem Content="No Time Limit"/>
                    <ComboBoxItem Content="5 minutes"/>
                    <ComboBoxItem Content="10 minutes"/>
                    <ComboBoxItem Content="15 minutes"/>
                    <ComboBoxItem Content="30 minutes"/>
                </ComboBox>
            </StackPanel>
            
            <CheckBox x:Name="ShowAIThinkingCheckBox" 
                      Content="Show AI Thinking Process" 
                      Foreground="White" 
                      FontSize="14" 
                      Margin="0,15" 
                      HorizontalAlignment="Center"
                      IsChecked="False"/>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                <Button Content="Start Game" 
                        Style="{StaticResource SettingsButton}"
                        Background="#E74C3C"
                        Command="{Binding ConfirmAIGameCommand}"/>
                <Button Content="Back" 
                        Style="{StaticResource SettingsButton}"
                        Background="#95A5A6"
                        Command="{Binding BackToMainMenuCommand}"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
