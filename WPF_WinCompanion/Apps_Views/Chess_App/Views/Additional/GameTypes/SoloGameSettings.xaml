<UserControl x:Class="WPF_WinCompanion.Apps_Views.Chess_App.Views.Additional.GameTypes.SoloGameSettings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WPF_WinCompanion.Apps_Views.Chess_App.Views.Additional.GameTypes"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="SettingsButton" TargetType="Button">
            <Setter Property="Background" Value="#3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}"
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="#80000000"
            VerticalAlignment="Center"
            Height="300"
            CornerRadius="15"
            BorderBrush="#27AE60"
            BorderThickness="3">
        <Border.Effect>
            <DropShadowEffect Color="Black" 
                              Direction="315" 
                              ShadowDepth="10" 
                              Opacity="0.8"
                              BlurRadius="15"/>
        </Border.Effect>
        
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="20">
            <TextBlock Text="🏁 Solo Game Settings" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Foreground="White" 
                       HorizontalAlignment="Center" 
                       Margin="0,0,0,20"/>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
                <TextBlock Text="Time Control:" 
                           FontSize="16" 
                           Foreground="White" 
                           VerticalAlignment="Center" 
                           Margin="0,0,10,0"/>
                <ComboBox x:Name="TimeControlComboBox" 
                          Width="120" 
                          SelectedIndex="0">
                    <ComboBoxItem Content="No Time Limit"/>
                    <ComboBoxItem Content="5 minutes"/>
                    <ComboBoxItem Content="10 minutes"/>
                    <ComboBoxItem Content="15 minutes"/>
                    <ComboBoxItem Content="30 minutes"/>
                </ComboBox>
            </StackPanel>
            
            <CheckBox x:Name="ShowHintsCheckBox" 
                      Content="Show Move Hints" 
                      Foreground="White" 
                      FontSize="14" 
                      Margin="0,15" 
                      HorizontalAlignment="Center"
                      IsChecked="True"/>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                <Button Content="Start Game" 
                        Style="{StaticResource SettingsButton}"
                        Background="#27AE60"
                        Command="{Binding ConfirmSoloGameCommand}"/>
                <Button Content="Back" 
                        Style="{StaticResource SettingsButton}"
                        Background="#E74C3C"
                        Command="{Binding BackToMainMenuCommand}"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
